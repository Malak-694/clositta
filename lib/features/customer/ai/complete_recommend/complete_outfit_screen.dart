import 'package:flutter/material.dart';

class CompleteOutfitScreen extends StatelessWidget {
  const CompleteOutfitScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}